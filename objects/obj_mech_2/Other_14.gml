/// @description TUTORIAL 9: fill coolant

ds_list_add(dialog, "SUPERVISOR: Now we're going to apply a patch to the hole.");
ds_list_add(dialog, "SUPERVISOR: Pick up a patch from the pile on the right and cover the hole with it.");
ds_list_add(dialog, "SUPERVISOR: Then pick up the orange welder with a different grabber.");
ds_list_add(dialog, "SUPERVISOR: Then maneuver it over the patch and activate it.")